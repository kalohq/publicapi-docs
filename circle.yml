dependencies:
  override:
    - pip install awscli==1.11.139

test:
  override:
    - test 0 = 0

deployment:
  master:
    branch: master
    commands:
      - aws s3 sync . s3://developer-docs.kalohq.ink --delete
      - aws s3 sync . s3://developer-docs.kalohq.com --delete
